# HyperFrames Composition Project

## Commands

```bash
npx hyperframes lint
npx hyperframes preview
npx hyperframes render
```

## Rules

- Main composition lives in `index.html`.
- Timelines are constructed synchronously.
- No `repeat: -1`.
- Visible animation must differ meaningfully across the requested frame checks.
